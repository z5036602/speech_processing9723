clear;clc
[audio,fs]= audioread('sample1.wav');
soundsc(audio,fs);
figure;
plot(audio);
title('audio time domain signal');
STFT_window_length = 100;
incre_dura = 10/1000;
assert(STFT_window_length)
spectrogram(audio,fs,STFT_window_length,incre_dura);